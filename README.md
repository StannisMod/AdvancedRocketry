# Forge Test Framework

Reusable testing infrastructure for Forge 1.12.2 mods. The project is intended to be consumed as a Gradle composite build by a mod project.

See [TEST_FRAMEWORK.md](TEST_FRAMEWORK.md) for the framework summary and extension points.

## Local commands

Use Java 8.

```bash
./gradlew test
./gradlew build
```
