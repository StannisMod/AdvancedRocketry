# Local per-machine test tuning — NOT committed (see .gitignore).
# See test.env.example for the canonical schema and tuning guidance.

# This host: 6 simultaneous client harness JVMs is comfortable.
clientForks=6
